# Clean Project Fixture

This fixture is a fully working Python project with all tests passing (exit code 0).
Used by JEVON E2E tests to verify:
1. Baseline clean assessment (`DONE` action chosen when clean).
2. VerificationEngine returning `VerificationOutcome(passed=True)`.
3. High confidence score (>0.95).
